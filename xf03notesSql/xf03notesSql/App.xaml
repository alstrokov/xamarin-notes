<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="xf03notesSql.App">
  <Application.Resources>
    <Thickness x:Key="PageMargin">20</Thickness>

    <Color x:Key="AppBgColor">AliceBlue</Color>
    <Color x:Key="NavBarColor">#1976D2</Color>
    <Color x:Key="NavBarTextColor">White</Color>

    <Style TargetType="{x:Type NavigationPage}">
      <Setter 
        Property="BarBackgroundColor"
        Value="{StaticResource NavBarColor}"/>
      <Setter 
        Property="BarTextColor"
        Value="{StaticResource NavBarTextColor}"/>
    </Style>

    <Style 
      TargetType="{x:Type ContentPage}" 
      ApplyToDerivedTypes="True">
      <Setter 
        Property="BackgroundColor"
        Value="{StaticResource AppBgColor}"/>
    </Style>
  </Application.Resources>
</Application>